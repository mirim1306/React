# Programming 소스 코드
## 02_counter
vanilla HTML, JavaScript
## React 시작
```shell
npm create vite@latest .
```

### CounterApp
- `useState(초기값)`
- `onClick={()=>set함수(바꿀값)}`
- `onClick={()=>set함수((이전 state)=>이전 state + 1)}`
- `onClick={함수이믈}`